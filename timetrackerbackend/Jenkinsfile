pipeline {
    agent any

    triggers {
        pollSCM "H/15 * * * *"
    }

    stages {

        stage('Build and Test') {
        steps {
               dir("timetrackerbackend"){
               sh 'java -version'
               sh 'make test'
              }
            }
        }

    }
}