pipeline {
    agent any

    triggers {
        pollSCM "H/15 * * * *"
    }

    environment {
        TIMESHEET_KEY = credentials ('timesheet_key')
    }

    stages {

        stage('Deploy backend infrastructure') {
        steps {
               dir("terraform/backend"){
               sh 'make'
              }
            }
        }

    }
}