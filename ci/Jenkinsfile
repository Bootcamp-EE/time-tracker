pipeline {

    stages {
        stage('Clone') {
            git url: 'https://github.com/abhiShirbhate/time-tracker'
        }

        stage('Unit test') {
            steps{
                sh ''
            }
        }

        stage('build') {
            sh ''
        }

        stage('deploy') {

        }

    }
}
